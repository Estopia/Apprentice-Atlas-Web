name: Publish containers
on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:
permissions:
  contents: read
  packages: write
jobs:
  images:
    strategy:
      matrix:
        service: [site, cms]
    runs-on: ubuntu-latest
    outputs:
      site-digest: ${{ steps.digest.outputs.site }}
      cms-digest: ${{ steps.digest.outputs.cms }}
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          {
            registry: ghcr.io,
            username: '${{ github.actor }}',
            password: '${{ secrets.GITHUB_TOKEN }}',
          }
      - uses: docker/build-push-action@v6
        id: build
        with:
          context: .
          file: apps/${{ matrix.service }}/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}-${{ matrix.service }}:${{ github.sha }}
          provenance: true
          sbom: true
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}
      - id: digest
        run: echo "${{ matrix.service }}=${{ steps.build.outputs.digest }}" >> "$GITHUB_OUTPUT"
      - uses: anchore/scan-action@v6
        with:
          {
            image: 'ghcr.io/${{ github.repository }}-${{ matrix.service }}@${{ steps.build.outputs.digest }}',
            fail-build: true,
            severity-cutoff: high,
          }

  staging:
    needs: images
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Deploy exact image digests through Portainer
        run: curl --fail --silent --show-error -X POST "${{ secrets.PORTAINER_STAGING_WEBHOOK }}"

  production:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [images, staging]
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Deploy promoted digests after manual approval
        run: curl --fail --silent --show-error -X POST "${{ secrets.PORTAINER_PRODUCTION_WEBHOOK }}"
